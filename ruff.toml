line-length = 88
target-version = "py311"

[lint]
select = ["E", "F", "W"]
ignore = [
  "E501",   # line too long — handled by formatter, not linter
]

[lint.per-file-ignores]
"tests/**" = ["F841"]   # local variable assigned but never used — common in test fixtures
